---
title: "Providers"
description: "Connect Better Agent to model APIs."
order: 10
---

# Providers

Providers connect Better Agent to model APIs. A provider gives you typed models with declared capabilities. Better Agent uses those capabilities to gate features at the type level — tools, structured output, and modalities only appear when the model supports them.

## Use a provider

The pattern is the same across providers: create an instance, get a model, and pass it to an agent.

```ts
import { defineAgent } from "@better-agent/core";
import { createOpenAI } from "@better-agent/providers/openai";

const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const agent = defineAgent({
  name: "assistant",
  model: openai.model("gpt-4o"),
});
```

Every provider accepts `apiKey` and optional `baseURL` and `headers` for custom endpoints or proxies:

```ts
const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY,
  baseURL: "https://my-proxy.example.com/v1",
  headers: { "X-Custom-Header": "value" },
});
```

## Model helpers

`model()` auto-detects the model type from the ID and routes to the correct provider endpoint. This is the most common way to get a model.

```ts
const text = openai.model("gpt-4o");
const image = openai.model("gpt-image-1");
```

Providers also expose modality-specific helpers when you want the type system to enforce a specific capability:

```ts
const text = openai.text("gpt-4o");
const image = openai.image("gpt-image-1");
const audio = openai.audio("tts-1");
const transcription = openai.transcription("gpt-4o-transcribe");
const embedding = openai.embedding("text-embedding-3-small");
```

Not all providers support all helpers. `model()` is always available.

## Available providers

**OpenAI** — text, image, audio, transcription, embedding, and video models via `@better-agent/providers/openai`. Requires `OPENAI_API_KEY`. You can override the endpoint with `OPENAI_BASE_URL`.

**Anthropic** — text and multimodal response models via `@better-agent/providers/anthropic`. Requires `ANTHROPIC_API_KEY`.

**xAI** — text and image models via `@better-agent/providers/xai`. Requires `XAI_API_KEY`.
