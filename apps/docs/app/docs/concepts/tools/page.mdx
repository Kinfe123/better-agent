---
title: "Tools"
description: "Let agents take structured actions."
order: 9
---

# Tools

Tools let agents do work. Instead of guessing, the model calls your code through a structured contract with validated input. A tool starts as a shared contract — you then materialize it as a server tool, a client tool, or a hosted provider tool.

## Define a Tool

Use `defineTool()` to create a contract with a name, description, and input schema. Better Agent accepts any schema that follows the Standard Schema spec (Zod, Valibot, ArkType) as well as raw JSON Schema.

```ts
import { defineTool } from "@better-agent/core";
import { z } from "zod";

const getWeather = defineTool({
  name: "get_weather",
  description: "Get the current weather for a location",
  schema: z.object({
    location: z.string(),
    unit: z.enum(["celsius", "fahrenheit"]),
  }),
});
```

## Server Tools

Use `.server()` when the tool should run on the server. The handler receives validated input and a context object with `signal` and `emit`.

```ts
const getWeatherTool = getWeather.server(async ({ location, unit }, { signal }) => {
  const res = await fetch(
    `https://api.weather.example/${location}?unit=${unit}`,
    { signal }
  );
  return res.json();
});
```

Add it to an agent:

```ts
const assistant = defineAgent({
  name: "assistant",
  model: openai.model("gpt-4o"),
  tools: [getWeatherTool],
});
```

Use `emit` to send custom events to the stream while the tool runs — useful for progress updates in long-running tools.

## Client Tools

Use `.client()` when the tool should run on the user's device. The model calls it, Better Agent emits the call to the client, and the server waits for the client to return a result.

```ts
const showConfetti = defineTool({
  name: "show_confetti",
  description: "Shows a confetti animation in the UI",
  schema: z.object({ color: z.string() }),
});

const showConfettiTool = showConfetti.client();
```

Better Agent does not execute client tools on the server. It waits for the client to submit a result, then feeds that result back into the agent loop.

## Error Handling

When a server tool throws, the default behavior (`toolErrorMode: "return"`) sends the error message back to the model so it can recover or try a different approach. Set `toolErrorMode: "throw"` on the agent when a tool failure should stop the run immediately.

```ts
const strictAgent = defineAgent({
  name: "strict",
  model: openai.model("gpt-4o"),
  tools: [deployTool],
  toolErrorMode: "throw",
});
```

For custom recovery logic, use the `onToolError` hook on the agent definition.

## Next steps

For approval flows before a tool runs, see [Human in the Loop](/docs/concepts/hil). For connecting to external tools over the Model Context Protocol, see [MCP](/docs/concepts/mcp).
