---
title: "Audio Transcription"
description: "Audio Transcription explains the responsibility boundary around `openai/audio-transcription`."
order: 340
---

# Audio Transcription

Audio Transcription explains the responsibility boundary around `openai/audio-transcription`.
## What This Area Does
Audio Transcription describes behavior readers can rely on before they inspect source details.
## What It Actually Does
The behavior is inferred from named inputs and mappings in the implementation, so this page stays conservative until richer usage examples are available.

## Behavior Notes
- Recognizes package markers such as `provider`, `at`, `message`, `type`, `role`, `finishReason`, `kind`, `endpoint`, `path`.
- Normalizes inputs to values such as `provider` -> `openai`, `at` -> `openai.audio.transcription.map.fileMissing`, `message` -> `Failed to map OpenAI Audio Transcription request`, `at` -> `openai.audio.transcription.mapToRequest`, `type` -> `message`, `role` -> `assistant`, `finishReason` -> `stop`, `kind` -> `event`, and more.
## How To Approach It
- Start with the workflow or capability this area supports.
- Move to setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Keep implementation verification separate from the reader-facing explanation.
## Reader Path
1. Start with the Audio Transcription page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
