---
title: "Images"
description: "Images explains the responsibility boundary around `xai/images`."
order: 440
---

# Images

Images explains the responsibility boundary around `xai/images`.
## What This Area Does
Images describes behavior readers can rely on before they inspect source details.
## What It Actually Does
The behavior is inferred from named inputs and mappings in the implementation, so this page stays conservative until richer usage examples are available.

## Behavior Notes
- Recognizes package markers such as `type`, `message`, `finishReason`, `inputShape`, `replayMode`, `providerId`.
- Normalizes inputs to values such as `type` -> `image_url`, `message` -> `Failed to map xAI image request`, `finishReason` -> `stop`, `inputShape` -> `prompt`, `replayMode` -> `single_turn_persistent`, `providerId` -> `xai`, `message` -> `xAI image edits require at least one source image.`.
## How To Approach It
- Start with the workflow or capability this area supports.
- Move to setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Keep implementation verification separate from the reader-facing explanation.
## Reader Path
1. Start with the Images page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
