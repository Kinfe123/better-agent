---
title: "Responses"
description: "Responses explains the responsibility boundary around `xai/responses`."
order: 460
---

# Responses

Responses explains the responsibility boundary around `xai/responses`.
## What This Area Does
Responses describes behavior readers can rely on before they inspect source details.
## What It Actually Does
The behavior is inferred from named inputs and mappings in the implementation, so this page stays conservative until richer usage examples are available.

## Behavior Notes
- Recognizes package markers such as `incomplete`, `provider`, `at`, `type`, `message`, `inputShape`, `replayMode`.
- Normalizes inputs to values such as `incomplete` -> `completed`, `provider` -> `xai`, `at` -> `xai.responses.map.input.fileProvider`, `at` -> `xai.responses.map.input.unsupportedPart`, `type` -> `function`, `message` -> `Failed to map xAI Responses request`, `inputShape` -> `chat`, `replayMode` -> `multi_turn`.
## How To Approach It
- Start with the workflow or capability this area supports.
- Move to setup, configuration, guide, or API pages when you need commands, options, or import paths.
- Keep implementation verification separate from the reader-facing explanation.
## Reader Path
1. Start with the Responses page to understand what this area is for.
2. Review the setup, configuration, or guide page when the behavior depends on runtime context.
3. Use API reference only when you need exact commands, imports, or options.
