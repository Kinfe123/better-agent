---
title: "Agent"
description: "The fundamental building block of Better Agent."
order: 1
---

# Agent

An agent is the core unit of behavior in Better Agent. It brings together a model, instructions, tools, and runtime behavior into one typed definition. At minimum, an agent needs a `name` and a `model`.

```ts
// server.ts
import { defineAgent } from "@better-agent/core";
import { openai } from "./openai";

export const helloAgent = defineAgent({
  name: "hello",
  model: openai.model("gpt-4o"),
});
```

## Instructions and context

Use `instruction` for the agent's system behavior. It accepts a static string or a function driven by validated runtime context.

```ts
const writer = defineAgent({
  name: "writer",
  model: openai.model("gpt-4o"),
  instruction: "You are a concise technical writer.",
});
```

Use `contextSchema` to define typed, validated runtime data for a run — like the current user, plan, or workspace. Any Standard Schema library (Zod, Valibot, ArkType) works here.

```ts
import { z } from "zod";

const supportAgent = defineAgent({
  name: "support",
  model: openai.model("gpt-4o"),
  contextSchema: z.object({
    userId: z.string(),
    plan: z.enum(["free", "pro"]),
  }),
  instruction: (context) =>
    `Help user ${context.userId}. They are on the ${context.plan} plan.`,
});
```

## Tools

Tools let the agent take action. Pass a single tool or an array.

```ts
import { weatherTool, stockTool } from "./tools";

const assistant = defineAgent({
  name: "assistant",
  model: openai.model("gpt-4o"),
  tools: [weatherTool, stockTool],
});
```

When a tool fails, the default behavior sends the error back to the model so it can recover. Use `toolErrorMode: "throw"` to stop execution immediately instead. See [Tools](/docs/concepts/tools) for the full contract.

## Steps

Each step produces one model response. If that response includes tool calls, the results feed into the next step. The loop continues until the model finishes naturally, a stop condition is met, or the step limit is reached.

Use `maxSteps` to cap how many steps a run can take, and `stopWhen` to add your own termination logic.

```ts
const researcher = defineAgent({
  name: "researcher",
  model: openai.model("gpt-4o"),
  tools: [searchWeb, readPage],
  maxSteps: 5,
});
```

## Next steps

Register your agent in a `betterAgent()` app and mount the HTTP handler — see [API](/docs/concepts/api). To give agents structured actions, read [Tools](/docs/concepts/tools). For typed JSON responses, see [Structured Output](/docs/concepts/structured-output).
