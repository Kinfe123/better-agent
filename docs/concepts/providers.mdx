---
title: "Providers"
description: "Connect Better Agent to model APIs."
order: 12
---

# Providers

Providers connect Better Agent to model APIs. A provider gives you typed models with declared capabilities. Better Agent uses those capabilities to gate features at the type level — tools, structured output, and modalities only appear when the model supports them.

## Using a Provider

The pattern is the same across all providers: create an instance, get a model, pass it to an agent.

```ts
import { defineAgent } from "@better-agent/core";
import { createOpenAI } from "@better-agent/providers/openai";

const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const agent = defineAgent({
  name: "assistant",
  model: openai.model("gpt-4o"),
});
```

Every provider accepts at minimum `apiKey` and optional `baseURL` and `headers` for custom endpoints or proxies:

```ts
const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY,
  baseURL: "https://my-proxy.example.com/v1",
  headers: { "X-Custom-Header": "value" },
});
```

## model()

`model()` auto-detects the model type from the ID and routes to the correct provider endpoint. It's the most common way to get a model — unknown IDs default to the text endpoint.

```ts
const textModel = openai.model("gpt-4o");
const imageModel = openai.model("gpt-image-1");
```

## Modality Helpers

Providers expose modality-specific helpers for explicit model selection. Use these when you want the type system to enforce a specific capability:

```ts
const text = openai.text("gpt-4o");
const image = openai.image("gpt-image-1");
const audio = openai.audio("tts-1");
const transcription = openai.transcription("gpt-4o-transcribe");
const embedding = openai.embedding("text-embedding-3-small");
```

Not all providers support all helpers. `model()` is always available.

## Supported Providers

Better Agent ships adapters for three providers:

- **OpenAI** — text, image, audio, transcription, embedding, and video models. Set `OPENAI_API_KEY` and optionally `OPENAI_BASE_URL`.
- **Anthropic** — text and multimodal response models. Set `ANTHROPIC_API_KEY`.
- **xAI** — text and image models. Set `XAI_API_KEY`.

All three providers follow the same `createProvider({ apiKey })` → `.model(id)` pattern, so switching providers means changing two lines.
